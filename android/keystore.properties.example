# Copy this file to `keystore.properties` and fill in the values.
#
# IMPORTANT:
# - Do NOT commit `keystore.properties` or your keystore file.
# - `android/.gitignore` should already ignore them.
#
# Example:
# storeFile=C:\\path\\to\\doggerz-upload-key.jks
# storePassword=CHANGEME
# keyAlias=doggerz
# keyPassword=CHANGEME

storeFile=
storePassword=
keyAlias=
keyPassword=
