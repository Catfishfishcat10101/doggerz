import React from "react";

/**
 * Minimal placeholder to fix "return outside of function" syntax error.
 * Replace with your real Dog sprite logic later.
 */
export default function Dog({ className = "" }) {
  return <div className={className} aria-label="Dog sprite placeholder" />;
}
