/**
 * Deprecated.
 *
 * This file used to contain an alternate router implementation, which caused
 * confusion because the real app entry (`src/main.jsx`) uses `src/AppRouter.jsx`.
 *
 * Keep this file as a small compatibility shim so any stale imports keep working.
 */

export { default } from "./AppRouter.jsx";