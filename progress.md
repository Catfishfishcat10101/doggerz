Original prompt: Review every PNG file and sprite sheet, locate puppy idle assets, and fix idle rendering plus autonomous idle walking.

- Implemented anim-specific sprite sheet selection and dynamic sheet sizing in `src/components/dog/SpriteSheetDog.jsx`.
- Updated idle manifest row to 14 frames (and 14 columns) in `src/features/game/jrManifest.json`.
- Replaced `src/components/dog/DogPixiView.jsx` with a sprite-sheet renderer that supports autonomous idle wandering.
- Attempted Playwright run via develop-web-game workflow; blocked by execution policy when starting `npm run dev`.
- Updated sprite sheet renderer to support multi-row frame grids and set idle to 16 frames / 4x4 at 4 fps in `src/features/game/jrManifest.json`, matching the new 1024x1024 idle sheet.
- Removed unused `src/pixi/DogAnimationController.js` and `src/components/dog/PuppyPassport.jsx` after confirming no remaining references.
- Removed unused `src/components/layout/GameTopBar.jsx` after confirming no remaining references.
- Refactored `applyDecay` in `src/redux/dogSlice.js` into an explicit 5-stage rule pipeline: compute degradation -> environment modifiers -> compounding -> thresholds -> legacy events.
- Added personality profile derivation in src/logic/personalityProfile.js (core temperament, dynamic states, learned traits).\n- Added selectDogPersonalityProfile selector in src/features/game/dogSelectors.js.\n- Playwright run failed: dev server on 5173 responded to HTTP, but web_game_playwright_client.js timed out waiting for DOMContentLoaded (localhost and 127.0.0.1). Stopped the dev server process; no screenshots captured.
- Added live Personality Intel panel to MainGame using selectDogPersonalityProfile (core temperament, dynamic states, learned traits, Big Five, trust behavior, stress signals).\n- Tuned personality formulas and thresholds in src/logic/personalityProfile.js for trust tiers: 0-20 standoff, 20-40 look-and-return, 40-60 middle-yard, 60-80 fence-run-wag-bark, 80-100 jump/paw/ignore-distractions.\n- Added Big Five outputs, explicit stress signals (whaleEye, destructiveOutlets, standOff), and model version marker.\n- Persisted computed profile in Redux (state.dog.personalityProfile) by syncing in inalizeDerivedState; selectors now read persisted profile first with fallback derivation.
- Removed player-facing personality HUD approach.
- Added a one-time in-game Temperament milestone popup card in MainGame when reveal becomes ready.
- Temperament reveal timing updated from 3 days to 14 days in dogSlice.updateTemperamentReveal and useDogLifecycle.
- Kept Big Five internal-only in model logic; no UI surface renders Big Five now.
- UI cleanup audit completed; removed unused UI files and dead UI branch.
- Deleted: src/components/environment/PawPrintsBackground.jsx, src/components/ui/LongTermProgressionCard.jsx, src/App.css.
- Deleted unused branch: src/features/game/MechanicsPanel.jsx, src/features/game/TrainingPanel.jsx, src/features/game/useDogLifecycle.jsx, src/components/ui/VoiceCommandButton.jsx.
- Fixed hook dependency lint warnings in src/components/dog/DogPixiView.jsx.
- Validation after cleanup: npm run lint PASS, npm run build PASS.
- Updated src/components/dog/DogPixiView.jsx locomotion selection to always use canonical walk while moving (facing flip handles direction) to address stuck-paw moonwalk from directional sheets.
- MainGame UI pass: added explicit backyard viewport layers (sky, fence, grass, tree) so yard visuals remain visible behind dog and overlays.
- MainGame behavior pass: when scene is rainy night, force sleeping animation (deep_rem_sleep) and set dogIsSleeping true for expected resting behavior.
- MainGame HUD restored with Level, Age, Stage, Mood, Energy, Health, and Hunger readouts.
- Interaction sheet de-scoped from Store: removed premium pack purchase buttons from in-yard interactions.
- Yard viewport polish: stronger rain overlay when weather is rain/storm, larger visible tree, smaller dog scale and larger movement area.
- DogToy visual updated from plain yellow circle to tennis-ball style and hidden while sleeping.
- DogPixiView movement logic updated to reduce in-place rocking/jitter by enforcing minimum travel distance, larger settle threshold, and longer pauses.
- Skill system overhaul phase 1: introduced perk cost + min-level gates and explicit unlock checks in `src/logic/skillTree.js`.
- Skill system wiring updated: `dogSlice` now calculates spent points by perk cost and enforces new unlock checks.
- Skill Tree page updated to show dynamic perk cost and rule-based unlock messages from the new skill rules engine.
- Implemented Dog Radio playlist logic in src/features/audio/useDynamicMusic.js: station selection by events (holiday/adoption/growth), weather, mood, and time-of-day; playlist rotation on track end with fades; removed bell-heavy night theme from rotation.
- Playwright run for Dog Radio update blocked by policy when attempting to start dev server via Start-Process; tests not executed.
- Sprite audit: pup/adult manifest sheets are dimension-correct; all senior anim sheets are currently missing. Updated SpriteSheetDog fallback chain so senior requests now prefer adult anim/clean sheets before falling back to pup clean.
- Dog Radio fine-tune: expanded station playlists (events, weather variants, mood recovery/playful), added station hold hysteresis (90s) to reduce abrupt switching, and improved queue reshuffle behavior with weighted track entries and anti-immediate-repeat.
- Replaced SpriteSheetDog renderer with Pixi grid slicing + AnimatedSprite ticker playback. Loader now tries candidate URLs in order (stage anim, adult fallback for senior, stage condition, clean fallback) and falls back to static image on total load failure. Build passes.
- Implemented unpredictability system updates in `src/redux/dogSlice.js`: startup surprise events (digging + button heist recovery), variable pet/training outcomes, autonomous zoomies, AI dream cue trigger, selective regular-kibble sniff reject, and potty fake-outs.
- Wired new gameplay UI surfaces in `src/features/game/MainGame.jsx`: session surprise banner, hijacked/stolen action button states, resolution flow, outcome messaging, zzz particles, guilty paws cue, rain-tint overlay, tree sway, doghouse placeholder, hover anticipation (Lv10+), and house-vs-yard sleep placement.
- Fixed sprite rendering foundations in `src/components/dog/SpriteSheetDog.jsx` and `src/components/dog/DogPixiView.jsx`: robust frame slicing with atlas offsets, corrected facing flip baseline, better grounding (anchor + shadow), attention target pathing, and sleep spot positioning.
- Added prompt packs for asset/music generation: `docs/sprite-sheet-prompts.md` and `docs/dog-radio-prompts.md`.
- Asset cleanup for APK bloat: removed 113 unused `adult_*.png` sheets not in current manifest keep-set and deleted large/dead artifacts under `public/assets/sprites/jr` (`TexturePacker-7.12.0.msi`, `unnamed.jpg`, `spritesheet.png`, `puppy-bouncing.png`).
- Purged noisy debug logs from gameplay paths (`MainGame`, `DogAIEngine`, `firebase.js`, `PupContext`) to reduce runtime overhead on mobile.
- Validation: `npx eslint` on all touched files passes; `npm run build` passes; Playwright client run executed against `/game` with updated screenshot + render state (`output/web-game/shot-0.png`, `output/web-game/state-0.json`).
- Implemented environment realism pass in `src/features/game/MainGame.jsx`: local clock day/night switching on `#backyard`, moon overlay, snow weather overlay, winter-only Dogman sprite, summer lightning-bug particles + occasional snap cue, rare ambient event manager (day butterfly chase, night owl observe), and Rolla fallback ZIP `65401`.
- Added pseudo-3D depth behavior: dynamic dog wrapper z-index based on viewport Y plus new foreground bushes so top-of-yard movement can pass behind foreground shapes.
- Enhanced dog movement depth in `src/components/dog/DogPixiView.jsx`: y-based scale range now reaches up to `1.5` toward foreground.
- Updated ambience channel in `src/features/audio/useDynamicMusic.js` to prioritize a dedicated wind loop (`wind-trees-loop.mp3`) with runtime fallback chain; added `public/audio/wind-trees-loop.mp3`.
- Fixed new runtime warning/error from Playwright run: moved `onPositionChange` emission out of state updater in `DogPixiView` and removed manual AnimatedSprite ticking in `SpriteSheetDog` (using Pixi autoUpdate). Current Playwright `errors-0.json` now only shows expected Firebase permission errors in local env.
- Validation after this pass: `npx eslint src/features/game/MainGame.jsx src/components/dog/DogPixiView.jsx src/components/dog/SpriteSheetDog.jsx src/features/audio/useDynamicMusic.js` PASS; `npm run build` PASS; Playwright run updated `output/web-game/shot-0.png`, `state-0.json`, `errors-0.json`.
- Added DOM/CSS step-animation fallback in `src/components/dog/SpriteSheetDog.jsx` (dog container + sprite node, `steps()` animation loop, row offset support, facing flip) so rendering no longer degrades to a single static sticker when Pixi sheet rendering fails.
- Fixed Playwright-discovered render issue by removing manual AnimatedSprite `update()` ticking; `SpriteSheetDog` now relies on Pixi autoUpdate, which cleared the `_updateID` pageerror in `output/web-game/errors-0.json`.
- Re-validated after fallback work: eslint + build pass; latest Playwright artifacts still render `/game` and now report only expected Firebase permission errors in local dev.
- Added direct yard overlay controls in `src/features/game/MainGame.jsx`: `#ui-container`, `#energy-display`, and `#feed-button` now render above the dog viewport with a one-tap feed action.
- Added deterministic `quickFeed` reducer in `src/redux/dogSlice.js` and mapped `feed_quick` in `src/features/game/dogSelectors.js` so the overlay button restores energy to 100, reduces hunger, nudges happiness, and resolves to the expected feed/eat state without depending on premium inventory.
- `Feed Pup` click now triggers a short happy animation/speed bump and plays `bark.m4a` directly when SFX are enabled. Validation: eslint PASS, build PASS, Playwright screenshot shows the overlay; Playwright run did not explicitly click the new button, but no new runtime errors were introduced.
