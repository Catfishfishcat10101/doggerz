<<<<<<< HEAD
# Copy this file to .env.local and fill in values if you want Firebase enabled.
# Vite only exposes vars prefixed with VITE_.

# Optional: silence missing-env warnings during development
VITE_SUPPRESS_ENV_MISSING_WARNINGS=false

# Firebase (optional)
=======
# Doggerz environment variables (Vite)
#
# Copy this file to .env.local and fill in values.
# NOTE: .env.local is gitignored by default.

# --- Firebase (required for Auth / Firestore cloud sync) ---
# Create a Firebase Web App and paste the config keys here.
>>>>>>> master
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Optional
VITE_FIREBASE_MEASUREMENT_ID=
<<<<<<< HEAD
=======

# --- Weather (optional) ---
# Enables ZIP-based local time + live weather tinting.
VITE_OPENWEATHER_API_KEY=
# Optional default ZIP if the player hasn't set one.
VITE_WEATHER_DEFAULT_ZIP=

# --- Dev / diagnostics knobs ---
# Optional: canonical site URL for generating absolute policy/support URLs.
# Example: https://doggerz.app/
VITE_SITE_URL=

# Silence "Missing env var" warnings in development.
VITE_SUPPRESS_ENV_MISSING_WARNINGS=false

# Allow debug overlays and sprite diagnostics in production.
# Keep false for release builds.
VITE_ENABLE_DEBUG=false

# Allow console.warn output in production.
# Keep false for release builds (errors are still captured).
VITE_ENABLE_PROD_WARNINGS=false
>>>>>>> master
